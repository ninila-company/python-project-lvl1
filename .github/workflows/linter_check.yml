name: linter_check
on: [push]
jobs:
  linter-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pip install poetry
      - run: pip install flake8
      - run: make lint
